// This component is no longer used — authentication is email-only.
// Kept as an empty export to avoid breaking imports during transition.
// Safe to delete once all references are removed.

export default function AuthMethodToggle() {
  return null;
}
